extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action='/submit/' + id, method='POST', enctype='multipart/form-data')
    input(type='file', name='objects')
    input(type='submit' value="Upload")
